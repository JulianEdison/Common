namespace JulianEdisonArt
{
    public enum Quality { Bad, Fine, Good, Great }
    public enum Rarity { NA, Common, Uncommon, Rare, Epic }
    public enum AreaScope { NA, Self, Adjacent, Horizontal, Vertical, Diagonal, Surrounding }
    public enum Amount3 { Low, Medium, High, }
    public enum Amount5 { None, Low, Medium, High, Full }
    public enum Alignment { Neutral, Good, Bad }
    public enum Difficulty { Given, VeryEasy, Easy, Medium, Hard, VeryHard, Impossible }
    public enum Day { Sunday, Monday, Tuesday, Wednesday, Thursday, Friday, Saturday }
}
